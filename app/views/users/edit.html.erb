<h1>Atualizando seus dados</h1>
<% @user.password = @user.password_confirmation = nil %>

<%= error_messages_for :user %>
<% form_for :user, :url => object_url, :html => { :method => :put } do |f| -%>
  <%= render :partial => "form", :locals => { :f => f } %>
  <p>
    <%=submit_tag "Atualizar"%>
  </p>
<% end -%>
