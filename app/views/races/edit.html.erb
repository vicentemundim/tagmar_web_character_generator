<h1>Editando Raça</h1>

<%= error_messages_for :race %>

<% form_for(:race, :url => object_url, :html => { :method => :put }) do |f| %>
  <%= render :partial => "form", :locals => { :f => f } %>
  <p>
    <%=submit_tag "Atualizar"%>
  </p>
<% end %>

<br/>

<%= link_to 'Exibir', object_url %>
|
<%= link_to 'Voltar', collection_url %>