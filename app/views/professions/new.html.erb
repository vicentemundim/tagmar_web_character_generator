<h1>Nova Profissão</h1>

<%= error_messages_for :profession %>

<% form_for(:profession, :url => collection_url) do |f| %>
  <%= render :partial => "form", :locals => { :f => f } %>
  <p>
    <%= submit_tag "Criar" %>
  </p>
<% end %>
<br/>
<%= link_to 'Voltar', collection_url %>